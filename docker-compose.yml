#version: "3.9"
services:
  django_app:
    build: .
    container_name: django_app
    command: gunicorn practica_despliegue.wsgi:application --bind 0.0.0.0:8000
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
